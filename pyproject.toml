[tool.poetry]
name = "gatorgrade"
version = "0.1.0"
description = ""
authors = ["Lauren Nevill <nevill01@allegheny.edu>"]

[tool.poetry.dependencies]
python = "\">=3.7\""

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
